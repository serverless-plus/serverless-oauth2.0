org: slsplus
app: oauth2
stage: dev

component: layer
name: oauth2-layer

inputs:
  region: ${env:REGION}
  name: ${name}
  src:
    src: ../node_modules
    targetDir: node_modules
  runtimes:
    - Nodejs12.16
